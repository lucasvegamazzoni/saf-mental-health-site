import './Companion.css';

/**
 * Companion — stub. Rendered in Layout just above the emergency help block.
 * The companion feature agent replaces this; until then it renders nothing.
 */
export default function Companion() {
  return null;
}

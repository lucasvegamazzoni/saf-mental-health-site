/** Stub — the recognition feature agent replaces this with the real pending-recognitions queue. */
export default function RecognitionsQueue() {
  return (
    <div className="mod-stub" role="status">
      <p className="mod-stub-kicker">Recognitions</p>
      <p>The recognitions queue isn't built yet. Nothing to review here for now.</p>
    </div>
  );
}

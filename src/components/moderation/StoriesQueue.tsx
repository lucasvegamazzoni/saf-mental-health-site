/** Stub — the stories feature agent replaces this with the real pending-stories queue. */
export default function StoriesQueue() {
  return (
    <div className="mod-stub" role="status">
      <p className="mod-stub-kicker">Stories</p>
      <p>The stories queue isn't built yet. Nothing to review here for now.</p>
    </div>
  );
}
